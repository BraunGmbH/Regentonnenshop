{% set isNameEmpty = trans("Regentonnenshop::Template.itemList" ~ itemListNo ~ "ListName") == 'Name' or
                     trans("Regentonnenshop::Template.itemList" ~ itemListNo ~ "ListName") is empty %}

{% if listType == "last_seen" %}
    {% if isNameEmpty %}
        {% set header = trans("Regentonnenshop::Template.itemListLastSeen") %}
    {% else %}
        {% set header = trans("Regentonnenshop::Template.itemList" ~ itemListNo ~ "ListName") %}
    {% endif %}
{% elseif listType == "cross_selling" %}
    {% if isNameEmpty %}
        {% set header = trans("Regentonnenshop::Template.itemListX" ~ config("Ceres.item.lists.cross_selling_type")) %}
    {% else %}
        {% set header = trans("Regentonnenshop::Template.itemList" ~ itemListNo ~ "ListName") %}
    {% endif %}
{% elseif listType == "tag_list" %}
    {% if isNameEmpty %}
        {% set header = "Tags" %}
    {% else %}
        {% set header = trans("Regentonnenshop::Template.itemList" ~ itemListNo ~ "ListName") %}
    {% endif %}
{% endif %}
